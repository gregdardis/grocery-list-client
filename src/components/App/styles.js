import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  buttonContainer: {
    width: '100%'
  },
  container: {
    alignItems: 'center',
    backgroundColor: '#fff',
    flex: 1,
    padding: 24
  },
  title: {
    fontSize: 32,
    fontWeight: 'bold'
  }
});